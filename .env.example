# Environment configuration for FFmpeg builder
# Copy this file to .env and customize as needed

# FFmpeg version (git tag) - leave empty for latest release
FFMPEG_VERSION=

# Android configuration
ANDROID_NDK_HOME=/path/to/android-ndk
ANDROID_MIN_SDK=21

# iOS configuration  
IOS_MIN_VERSION=13.0
